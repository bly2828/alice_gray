<h1>Here are all my actors:</h1>

<% @actors.each do |actor| %>
  <p><%= link_to actor.first_name + ' ' + actor.last_name, actor_path(actor) %></p>
  <%= image_tag actor.profile_pic, style: 'width: 150px' %>
<% end %>

<%= link_to 'Go Home', root_path %>
